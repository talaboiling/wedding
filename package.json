{
  "name": "invitation-backend",
  "version": "1.0.0",
  "description": "Vercel backend for invitation",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@vercel/kv": "^3.0.0"
  }
}
